# Aliyda

Aliyda, banka ekstrelerini (PDF) analiz eden yapay zeka destekli bir kişisel finans asistanıdır.

Bu proje, bir monorepo yapısında geliştirilmektedir. `backend` klasörü FastAPI tabanlı Python API'sini, `frontend` klasörü ise React (Vite) tabanlı kullanıcı arayüzünü içerir.
